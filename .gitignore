node_modules/
dist/
.env
__pycache__/
*.pyc
nohup.out
.DS_Store

# Nothing under images/ is ignored. An earlier rule excluded every SVG here
# to keep throwaway placeholders out of the repo, but the W13 counting images
# are real content (their item counts are the widget's answer key), so they
# were missing from the deployed site while still present locally.
# Regenerate placeholders with scripts/make_placeholders.py if they are lost.
dist-preview/
